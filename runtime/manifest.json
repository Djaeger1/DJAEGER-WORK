{
  "version": "v2.5.40-snapshot-integrity",
  "description": "Repairs DJAEGER WORK live snapshot integrity by exposing the full bridge snapshot over the trusted remote tunnel and making Railway consume that authoritative snapshot instead of the reduced status endpoint.",
  "restart": false
}
