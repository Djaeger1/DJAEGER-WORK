{
  "version": "v2.5.31-youtube-trusted-remote-auth",
  "description": "YouTube thumbnail remote-auth repair: trusted outbound Railway tunnel may retry per-video thumbnails without exposing the local admin token; direct local/API callers still require the admin token and the existing local-or-trusted-remote gate remains enforced.",
  "restart": false
}
