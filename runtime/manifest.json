{
  "version": "v2.5.42-youtube-analytics-consent",
  "description": "Adds secure incremental YouTube Analytics OAuth consent using loopback PKCE on the dashboard, stores the upgraded refresh token only on Redmi 5A, and triggers real retention/CTR/watch-time sync without exposing OAuth secrets.",
  "restart": false
}
