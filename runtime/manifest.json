{"version":"v2.5.11-recovery-safe","description":"DJAEGER WORK safe recovery: serializes runtime handoff with supervisor lock, hard-stops only stale HERMES WORK listeners after graceful timeout, keeps single-updater PID lock, makes telemetry read-only, and moves full PSS/kernel/zRAM accounting to a private on-demand memory audit endpoint","restart":false}
